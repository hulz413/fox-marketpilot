## 1. 示例数据与启动逻辑

- [x] 1.1 将现有中文示例从字符串升级为结构化示例对象，覆盖 brief、budget、target_channels、preferred_categories、excluded_categories、target_audience、expected_profit、supply_preferences 和 constraints。
- [x] 1.2 保留面向展示的示例标题或摘要，使空状态、新建研究页和按钮文案可以复用同一份示例数据。
- [x] 1.3 抽取或复用“创建研究任务后启动运行并跳转进度页”的前端逻辑，确保示例启动和自定义提交共用同一真实任务流程。

## 2. 产品入口与新建研究页

- [x] 2.1 更新研究任务空状态，使 2-3 个中文示例从静态文本变成可直接启动的操作入口。
- [x] 2.2 更新新建研究页面，使示例既可以填入表单，也可以直接启动对应示例研究。
- [x] 2.3 为示例启动添加 pending、禁用重复点击和中文错误展示，避免重复创建任务或静默失败。
- [x] 2.4 确保示例启动成功后进入 `/research/tasks/{taskUuid}`，后续继续沿用已有进度、商机、报告、来源和需求洞察入口。

## 3. 边界与体验验证

- [x] 3.1 确认示例任务保存为普通真实研究任务，不新增静态 demo 结果、专用 demo endpoint 或特殊持久化模型。
- [x] 3.2 确认手机和窄屏下示例入口不造成页面级横向滚动，且启动操作在卡片或列表项中可见。
- [x] 3.3 确认 API 或 worker 不可用时展示中文错误，不展示静态商机或报告作为替代。

## 4. 测试与验收

- [x] 4.1 补充或更新前端 UI contract 测试，覆盖空状态示例入口、示例填入和示例启动的关键文案/操作。
- [x] 4.2 运行前端 lint 或现有前端测试，确认示例数据结构变更没有破坏现有页面。
- [x] 4.3 运行后端相关测试或确认无需后端变更，确保研究任务创建和启动 API 仍按既有契约工作。
- [x] 4.4 本地演示验证：从无任务空状态启动一个中文示例，确认任务进入进度页并产生正常 run 状态和阶段事件。
- [x] 4.5 运行 `openspec validate start-demo-research --strict` 并修复发现的问题。
