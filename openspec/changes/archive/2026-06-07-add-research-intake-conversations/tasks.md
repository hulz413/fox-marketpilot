## 1. 后端数据模型和迁移

- [x] 1.1 新增 `research_intake_conversations` 和 `research_intake_messages` 模型，包含公开 UUID、通用时间字段、软删除字段和不对外暴露的内部自增 ID
- [x] 1.2 新增 Alembic 迁移，创建会话表、消息表、必要索引和研究任务关联外键
- [x] 1.3 新增 repository/service 基础读写能力，默认只读取 `deleted_at` 为空的会话和消息
- [x] 1.4 新增 Pydantic schema，覆盖会话创建、消息提交、草稿读取、确认创建和安全错误响应

## 2. 对话整理和任务确认

- [x] 2.1 实现研究需求草稿标准化逻辑，将聊天内容整理为兼容 `ResearchTaskCreate` 的字段
- [x] 2.2 实现 LLM 对话整理服务，要求固定 JSON 输出助手回复、草稿增量、缺失字段、默认假设和可创建状态
- [x] 2.3 实现 local/test 环境确定性 fallback，保证自动化测试不依赖外部 LLM
- [x] 2.4 实现最小可创建规则，区分阻断字段、可默认字段和非阻断补充字段
- [x] 2.5 实现会话确认创建研究任务，并复用现有研究任务创建与启动运行逻辑
- [x] 2.6 处理重复确认、启动失败、模型输出无效和不可创建会话等错误分支

## 3. API 和可观测性

- [x] 3.1 新增创建会话、读取会话、发送消息、更新需求和确认创建研究任务的 API 路由
- [x] 3.2 确保 API 响应只暴露公开 UUID，不暴露内部自增 ID、完整 prompt、模型原始输出或异常堆栈
- [x] 3.3 为消息处理和确认创建记录应用日志，包含会话 UUID、消息 UUID、阶段、状态、耗时和安全错误摘要
- [x] 3.4 接入可用的 tracing metadata；tracing 未启用时 API 仍保持成功或返回中文可理解错误

## 4. 前端聊天页面

- [x] 4.1 在首页新建研究工作台新增聊天模式，展示聊天消息、输入框、研究草稿、缺失字段、默认假设和确认启动状态
- [x] 4.2 新增前端 API client 和 TanStack Query/mutation hooks，支持创建会话、发送消息、读取会话、更新需求和确认启动
- [x] 4.3 在新建研究体验中提供表单模式和聊天模式入口，保持全局导航仍为新建研究和我的研究
- [x] 4.4 实现聊天思考中、更新中、整理失败、不可创建、可创建、确认启动中和启动失败等用户可见状态
- [x] 4.5 完成小屏布局，确保聊天输入、主操作和草稿摘要不造成页面级横向滚动
- [x] 4.6 将首页默认体验调整为聊天式新建研究，表单和聊天使用同一路由内模式切换，保留 `/research/tasks` 作为我的研究列表，并支持左上角 logo 返回首页
- [x] 4.7 模式切换时保留聊天和表单已填写内容，不因切换可见模式重置未提交状态
- [x] 4.8 在最新商机顾问追问下方展示可点选短回复，点击后按普通用户消息发送
- [x] 4.9 识别“信息够了”“先更新需求”等用户自然语言意图，直接整理需求而不是继续追问
- [x] 4.10 将聊天记录区域适配浏览器可用高度并支持内部滚动，最新消息自动滚动到可见位置
- [x] 4.11 将研究草稿面板与聊天卡等高适配视口，并支持内部垂直滚动，避免字段列表撑高整页
- [x] 4.12 在同一浏览器会话中恢复未确认的聊天对齐会话，避免刷新后丢失上下文
- [x] 4.13 将候选短回复改为 API 返回字段，前端只展示最新助手消息的候选词
- [x] 4.14 提供把聊天草稿带入表单编辑的入口，允许用户在确认前直接修正结构化字段
- [x] 4.15 为更新需求 LLM 分析增加超时和 fallback，避免按钮长时间卡住
- [x] 4.16 候选短回复只跟随明确追问型助手消息出现，避免信息已足够时展示无关候选词

## 5. 测试和验证

- [x] 5.1 添加后端模型、schema、草稿标准化、就绪判断和 fallback 单元测试
- [x] 5.2 添加 API 测试，覆盖创建会话、发送消息、更新需求、读取会话、确认创建、重复确认和错误安全摘要
- [x] 5.3 添加前端组件或页面测试，覆盖聊天发送、草稿更新、确认启动和错误状态
- [x] 5.4 运行后端测试、前端 lint/typecheck 和相关单元测试
- [x] 5.5 启动本地服务，验证首页聊天模式到任务进度页的端到端演示路径
- [x] 5.6 验证聊天阶段不会创建商机结果、来源、RAG evidence、报告或投递基础研究任务，直到用户确认启动
- [x] 5.7 验证首页默认聊天、logo 回首页和后续聊天补充会增量更新草稿
