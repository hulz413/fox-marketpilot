## 1. 数据模型

- [x] 1.1 将需求洞察相关表命名统一为 `demand_insights` 和 `demand_insight_sources`，并同步模型、外键和索引引用
- [x] 1.2 新增 `supply_candidates` 迁移，包含 UUID、任务/商机外键、排序、候选字段、来源状态、时间戳和软删除字段
- [x] 1.3 新增 `supply_candidate_sources` 迁移，包含 UUID、货源候选/来源外键、关联说明、时间戳和软删除字段
- [x] 1.4 新增 SQLAlchemy 模型和索引，支持按任务、商机和候选排序读取未软删除货源候选
- [x] 1.5 新增 Pydantic 模式，覆盖生成结果、读取响应、来源摘要、来源状态、字段长度和谨慎表达校验

## 2. 后端服务

- [x] 2.1 新增货源候选仓储函数，支持按任务替换、按任务列表读取、按商机读取和软删除
- [x] 2.2 实现来源选择辅助逻辑，优先使用关联到当前商机的未软删除 `supply` 和 `general` 来源
- [x] 2.3 实现本地和测试环境的确定性回退货源候选生成
- [x] 2.4 实现结构化 LLM 货源候选生成，包含批量提示词、JSON 解析、Pydantic 校验和一次可控重试
- [x] 2.5 确保货源候选和来源关联说明使用谨慎语气，并拦截“已确认供给”“库存已确认”等结论文案

## 3. 研究运行

- [x] 3.1 将 `generate_supply_candidates` 加入研究任务阶段枚举和前端阶段中文映射
- [x] 3.2 在 `generate_demand_insights` 之后新增 LangGraph 节点，用于生成并持久化货源候选
- [x] 3.3 记录货源候选阶段事件、耗时、安全错误摘要、应用日志和 LangSmith trace 元数据
- [x] 3.4 商机已持久化后，即使货源候选生成失败，也保持任务状态为 `completed`
- [x] 3.5 确保重新运行任务后，默认读取不会返回旧货源候选和旧来源关联

## 4. API

- [x] 4.1 新增 `GET /api/v1/research-tasks/{task_uuid}/supply-candidates`，返回任务级货源候选列表
- [x] 4.2 新增 `GET /api/v1/opportunities/{opportunity_uuid}/supply-candidates`，返回单个商机货源候选列表
- [x] 4.3 返回来源摘要时只暴露公开来源 UUID，不暴露任何内部自增 ID
- [x] 4.4 任务或商机没有货源候选时返回成功空列表；资源不存在或已软删除时返回 404

## 5. 前端

- [x] 5.1 新增前端 API 类型和读取函数，支持任务级和商机级货源候选
- [x] 5.2 在商机详情页新增货源候选面板，展示候选名称、供给市场、搜索关键词、价格区间、起订量、规格参考、确认问题、来源状态和来源链接
- [x] 5.3 在任务报告页新增货源候选摘要，并提供进入商机详情查看完整候选的入口
- [x] 5.4 当货源候选缺失或读取失败时，展示非阻塞中文空状态和错误状态
- [x] 5.5 确保 UI 文案保留“候选”“初步参考”“待确认”“待验证”等谨慎表达，避免“已确认供给”类结论文案

## 6. 测试与验证

- [x] 6.1 增加后端测试，覆盖货源候选持久化、重新运行替换、来源关联和只返回公开标识
- [x] 6.2 增加后端测试，覆盖确定性回退生成、结构化校验失败处理和运行失败非阻塞行为
- [x] 6.3 增加进度和事件测试，覆盖 `generate_supply_candidates` 阶段
- [x] 6.4 增加前端 UI 契约测试，覆盖货源候选面板、谨慎文案和空状态
- [x] 6.5 运行后端测试、前端 lint/测试和 `suggest-supply-candidates` 的 OpenSpec 校验
- [x] 6.6 本地完成一次从新建研究到报告和商机详情的演示验证，确认货源候选正确渲染
