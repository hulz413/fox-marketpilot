## 1. 后端数据与快照

- [x] 1.1 新增 Alembic 迁移，创建 `report_shares` 表，包含内部 `id`、公开 `uuid`、`research_task_id`、`share_token`、标题、报告快照 JSON、状态、撤销时间、时间戳和软删除字段。
- [x] 1.2 新增报告分享后端模块，包含 SQLAlchemy model、Pydantic schema、repository 和默认排除软删除记录的读取函数。
- [x] 1.3 实现高熵 URL-safe 分享 token 生成，并确保分享公开读取不使用任务 UUID 作为访问凭证。
- [x] 1.4 实现报告快照构建服务，白名单提取任务、商机、来源、需求洞察、货源候选、竞品参考、验证预算、风险复核和行动计划字段。
- [x] 1.5 确保快照和公开响应不包含内部自增 ID、run ID、trace ID、trace URL、阶段事件、错误堆栈、对象存储配置、LLM/provider 凭证或内部日志。

## 2. 分享 API

- [x] 2.1 实现创建分享服务，校验任务存在、未软删除且已有商机结果，保存 `active` 分享记录和报告快照。
- [x] 2.2 实现任务级分享列表读取服务，按稳定顺序返回当前未软删除分享记录，不暴露内部自增 ID。
- [x] 2.3 实现撤销分享服务，将分享状态标记为 `revoked` 并保存撤销时间。
- [x] 2.4 实现公开分享读取服务，仅允许通过有效、未撤销、未软删除的分享 token 读取报告快照。
- [x] 2.5 新增报告分享 API 路由，提供创建分享、读取任务分享列表、撤销分享和通过 token 公开读取接口，并挂载到 v1 router。

## 3. 前端分享体验

- [x] 3.1 在前端 API client 中新增报告分享类型、创建分享、读取任务分享列表、撤销分享和公开读取分享函数。
- [x] 3.2 在最终报告页新增分享组件，覆盖加载、生成中、生成成功、复制链接、打开分享页、撤销分享、失败和重试状态。
- [x] 3.3 新增公开分享报告路由，例如 `/share/reports/[token]`，使用只读阅读型布局渲染报告快照。
- [x] 3.4 分享页展示任务摘要、推荐排序、商机详情、需求洞察、货源候选、竞品参考、验证预算、风险复核、行动计划、来源线索和谨慎边界说明。
- [x] 3.5 分享页不展示重新运行、编辑、任务进度、LangSmith trace、后台调试或删除任务入口。
- [x] 3.6 确保报告页分享入口和公开分享页在桌面和手机宽度下不会造成页面级横向溢出。

## 4. 测试与验证

- [x] 4.1 增加后端测试，覆盖成功创建分享、分享 token 结构、快照字段、内部 ID 不暴露和公开读取。
- [x] 4.2 增加后端测试，覆盖增强数据缺失仍可分享、无商机结果拒绝分享、分享不存在和已撤销分享不可读取。
- [x] 4.3 增加重新运行测试，确认旧分享快照不会被新运行覆盖，再次分享会创建新的分享记录和 token。
- [x] 4.4 增加后端测试，确认公开分享响应不包含 run ID、trace URL、阶段事件、错误堆栈或内部配置字段。
- [x] 4.5 增加前端 UI 契约测试，确认报告页包含分享操作、复制链接、撤销入口、失败非阻塞文案和公开分享页只读布局。
- [x] 4.6 更新 `docs/mvp/roadmap.md`，记录 `share-final-report` 的进度、范围和在线分享方式。
- [x] 4.7 运行后端测试、前端测试、类型检查、lint 和 `openspec validate share-final-report --strict`。
- [x] 4.8 本地完成一次中文示例研究分享演示验证，确认报告页可生成分享链接，公开分享页可在线浏览且手机布局可读。
