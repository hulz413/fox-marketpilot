## 1. 后端进度读取模型

- [x] 1.1 增加 Agent 阶段事件和研究任务进度的公开读取 schema，包含任务字段、当前运行字段、阶段事件和可用操作提示。
- [x] 1.2 增加 repository/service 逻辑，按任务 ID 和 run ID 读取当前运行未软删除的 `agent_run_events`，并按 `started_at` 排序。
- [x] 1.3 增加 `GET /api/v1/research-tasks/{task_uuid}/progress`，返回组合后的进度读取模型；任务不存在或已软删除时返回 404。
- [x] 1.4 确保进度响应不暴露内部自增 ID、敏感凭证或原始异常堆栈。
- [x] 1.5 增加后端测试，覆盖未启动任务、已完成运行时间线、失败运行安全摘要、重新运行只展示当前 run、软删除事件过滤。

## 2. 前端进度 API 与页面

- [x] 2.1 增加前端 API 类型和 `fetchResearchProgress` 请求方法，与后端进度响应保持一致。
- [x] 2.2 在研究任务区域增加任务进度页路由，复用现有产品外壳和中文页面文案。
- [x] 2.3 构建进度摘要视图，展示任务需求、关键条件、状态、当前阶段、run ID、trace 链接和失败原因。
- [x] 2.4 构建阶段时间线视图，覆盖 queued、running、completed、failed 和阶段事件缺失状态，并使用稳定阶段文案。
- [x] 2.5 增加按状态展示的操作入口，包括启动、重新运行、LangSmith trace、商机推荐、基础报告和返回任务列表。

## 3. 导航与状态刷新

- [x] 3.1 调整新建研究提交流程，让创建并启动成功后直接跳转到新任务进度页。
- [x] 3.2 更新研究任务列表，让 created、queued、running 和 failed 任务有清晰的进度入口；completed 任务保留商机推荐和基础报告入口。
- [x] 3.3 在进度页增加 TanStack Query 轮询，仅当任务状态为 `queued` 或 `running` 时刷新，并在 `completed` 或 `failed` 后停止。
- [x] 3.4 补齐进度页的中文错误、加载、空阶段、未找到和重试状态。

## 4. 验证

- [x] 4.1 运行后端测试，覆盖研究任务、商机研究、LangSmith 集成和新增进度 API。
- [x] 4.2 运行前端 lint/typecheck，覆盖进度页、任务列表改动和 API 类型。
- [x] 4.3 手动验证本地 P0 流程：从中文示例创建任务、落到进度页、观察阶段更新、打开商机推荐、打开基础报告，并尽量验证失败任务重试路径。
- [x] 4.4 验证可观测性行为：trace 链接只在存在时展示，阶段时间线使用当前 run 数据，阶段事件缺失不会阻塞任务状态展示。
