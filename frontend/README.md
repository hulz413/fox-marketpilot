# MarketPilot Frontend

这是 MarketPilot 的 Next.js 前端应用，部署时使用 `frontend/` 作为 Vercel root directory。

## 常用命令

```bash
npm install
npm run dev
npm run lint
npm run typecheck
npm run build
```

本地开发默认连接 `http://localhost:8000` 的后端 API 和 SSE 服务。
